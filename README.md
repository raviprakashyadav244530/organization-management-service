# organization-management-service
A secure Organization Management Service that allows creating, updating, and deleting organizations with dynamic collections, admin authentication, and full CRUD operations using Node.js, TypeScript, MongoDB, and JWT.
